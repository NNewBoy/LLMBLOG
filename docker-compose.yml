services:
  backend:
    build:
      context: ./backend
    env_file:
      - ./backend/.env
    environment:
      - DB_PATH=app/data/app.db
      - UPLOAD_DIR=llmblog_uploads
    volumes:
      - db-data:/app/app/data
      - llmblog_uploads:/app/llmblog_uploads
    restart: unless-stopped

  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    ports:
      - "80:80"
    volumes:
      - llmblog_uploads:/opt/llmblog/backend/llmblog_uploads:ro
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  db-data:
  llmblog_uploads:
