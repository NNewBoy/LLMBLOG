# 复制为 .env 并修改
SECRET_KEY=change-me-to-a-long-random-string
ADMIN_PASSWORD_HASH=
# 前端预哈希后的密码（SHA256），留空则首次启动用明文 admin 初始化并打印
DB_PATH=app/data/app.db
UPLOAD_DIR=uploads
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
ACCESS_TOKEN_EXPIRE_MINUTES=120
